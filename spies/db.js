//assuming db.js has all the methods to read and write from the database 

//function to save user
saveUser = (user) => {
    //not a real function, just an ilustration
    console.log('Saving user', user)
}

//exporting the function
module.exports = {
    saveUser
}